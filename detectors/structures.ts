/**
 * Enum representing the risk rating of a finding.
 */
export enum RiskRating {
    Information = 'Information',
    Low = 'Low',
    Medium = 'Medium',
    High = 'High',
    Critical = 'Critical'
}